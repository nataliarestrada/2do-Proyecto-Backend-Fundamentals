<p class="font-bold">Listado de Mis Rentas {{session.username}}</p>
{{#if hasBook }}
    <ul>
        {{#each rentas}}
            <div class="flex m-3 bg-gray-200 gap-5">
                <div >
                    <h3 class="font-bold text-lg">{{this.id}}</h3>
                    <p>id_usuario: {{this.id_uduario}}</p> 
                    <p>id_libro: {{this.id_libro}}</p>
                    <p>fecha_inicio: {{this.fecha_inicio}}</p>
                    <p>fecha_fin: {{formatDate this.fecha_fin}}</p>
                    <p>fecha hoy:{{formatDate ../fecha_hoy}}</p>
                    <p class="text-blue-500">{{controlDate (formatDate this.fecha_fin) (formatDate ../fecha_hoy)}}</p>
                    <p>estado: {{this.estado}}</p>
                </div>

                {{#if (igual this.estado ../inactivo)}}
                    <div class="flex items-end ml-10">
                        <p>Libro devuelto</p>
                    </div>
                {{else}}
                    <div class="flex items-end ml-10">
                        <a href="/return_libro/{{this.id}}" class="rounded-md m-1 p-2 text-white bg-orange-300">Devolver</a>
                        
                        {{!-- {{#if (controlDate (formatDate this.fecha_fin) (formatDate ../fecha_hoy))}}
                            <p class="text-red-400">Renta vencida! Devuelve tu libro</p>
                        {{else}}
                            <p class="text-green-400">Renta activa</p>
                        {{if}} --}}

                    </div>

                {{/if}}


                
                
            </div>
        {{/each}}
    </ul>
{{else}}
    <p class="text-red-500">Ninguna Renta registrado</p>
{{/if}}